# Plancher 2.15.6 : les serveurs PRONOTE 2026 ne publient plus l'appel
# Start({...}) dans l'attribut onload du <body> ; les versions antérieures
# échouent à la connexion (« KeyError: 'onload' »). Borne haute <3 : suivre
# les correctifs 2.x sans subir un changement d'API majeur sans l'avoir testé.
# Vérifier la version installée : resources/venv/bin/pip show pronotepy
pronotepy>=2.15.6,<3
requests>=2.28

# Décodage de l'image du QR Code fourni par Pronote.
# zxing-cpp est livré en wheel autonome : aucune bibliothèque système à installer
# (contrairement à pyzbar, qui exige libzbar0). Python 3.9 (Debian 11) accepté.
zxing-cpp>=2.2
pillow>=10.0
